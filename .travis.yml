#
# This file has been generated by the Ibuildings QA-Tools
#
# Any custom Travis-ci configuration be added here
# This file should be added to your project repository
#

sudo: false

language: php

php:
  - 5.6
  - 7
  - hhvm

matrix:
  allow_failures:
    - php: hhvm
  fast_finish: true
  include:
    - php: 5.6
      env: [SYMFONY_VERSION="2.3.*"]
    - php: 5.6
      env: [SYMFONY_VERSION="2.7.*"]
    - php: 5.6
      env: [SYMFONY_VERSION="2.8.*"]
    - php: 5.6
      env: [SYMFONY_VERSION="3.0.*"]
    - php: 7.0
      env: [SYMFONY_VERSION="2.3.*"]
    - php: 7.0
      env: [SYMFONY_VERSION="2.7.*"]
    - php: 7.0
      env: [SYMFONY_VERSION="2.8.*"]
    - php: 7.0
      env: [SYMFONY_VERSION="3.0.*"]

cache:
  directories:
    - ~/.composer/cache/files

before_script:
  - if [ "$TRAVIS_PHP_VERSION" != "hhvm" ]; then phpenv config-add .travis.php.ini; fi
  - composer self-update
  - if [ "$SYMFONY_VERSION" != "" ]; then composer require --no-update symfony/config:${SYMFONY_VERSION} symfony/dependency-injection:${SYMFONY_VERSION} symfony/http-kernel:${SYMFONY_VERSION}; fi;
  - if [ "$SYMFONY_VERSION" != "" ]; then composer require --no-update --dev symfony/framework-bundle:${SYMFONY_VERSION}; fi;
  - composer install --prefer-dist

script:
  - ant

branches:
  only:
    - master
    - develop
